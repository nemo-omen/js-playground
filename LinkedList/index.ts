import { SingleLinkedList, ListNode } from "./LinkedList.ts";

const list = new SingleLinkedList();

list.push('First');
list.push('Second');
list.push('Third');
list.push('Whatever');
console.log(list);